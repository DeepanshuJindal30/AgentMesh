# Development secrets only — replace via sealed-secrets / External Secrets in real envs.
apiVersion: v1
kind: Secret
metadata:
  name: agentmesh-secrets
  namespace: agentmesh
  labels:
    app.kubernetes.io/part-of: agentmesh
type: Opaque
stringData:
  POSTGRES_USER: agentmesh
  POSTGRES_PASSWORD: agentmesh_dev_password
  POSTGRES_DB: agentmesh
  DATABASE_URL: postgresql+asyncpg://agentmesh:agentmesh_dev_password@postgres:5432/agentmesh
  DATABASE_URL_SYNC: postgresql+psycopg2://agentmesh:agentmesh_dev_password@postgres:5432/agentmesh
  RABBITMQ_USER: agentmesh
  RABBITMQ_PASSWORD: agentmesh_dev_password
  RABBITMQ_URL: amqp://agentmesh:agentmesh_dev_password@rabbitmq:5672//
  CELERY_BROKER_URL: amqp://agentmesh:agentmesh_dev_password@rabbitmq:5672//
  JWT_SECRET: change-me-agentmesh-dev-jwt-secret
